# Prevent direct access to uploaded files
# All images must be accessed through image_handler.php

# Deny all direct access
Order Deny,Allow
Deny from all

# Allow access from image_handler.php only
<Files ~ "\.(jpg|jpeg|png|gif)$">
    Order Deny,Allow
    Deny from all
</Files>
